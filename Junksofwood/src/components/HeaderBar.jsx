// import PropTypes from 'prop-types'

const Header = () => {
  return (
    <header className="header">
      <h1>
        Junks of Wood. This is the header. 
      </h1>
    </header>
  );
};

// Header.propTypes = {
//   title: PropTypes.string.isRequired
// };
export default Header;